/**
 * KirillPanel — панель Кирилла. Next-Gen Gaming HUD.
 * Spin Progress → Roulette → Missions.
 */
import { PilotsPanel } from './PilotsPanel'

export function KirillPanel(props) {
  return <PilotsPanel childId="kirill" accentColor="purple" {...props} />
}
